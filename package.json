{
  "name": "adhd-med",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ADHD MED — a generative frequency instrument for focus. Binaural beats, geometry and equations you can hear. No accounts, no backend, works offline.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "size": "node scripts/size.mjs",
    "verify": "node scripts/verify-dist.mjs",
    "check": "tsc --noEmit && vitest run && vite build && node scripts/verify-dist.mjs && node scripts/size.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^26.2.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
